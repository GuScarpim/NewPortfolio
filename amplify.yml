version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - cd front
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: /front/build
    files:
      - '**/*'
  cache:
    paths: []